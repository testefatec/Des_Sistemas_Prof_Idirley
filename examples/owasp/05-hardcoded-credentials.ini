; Exemplo comentado: credenciais hardcoded
; Este arquivo demonstra como credenciais podem aparecer em configuração — NÃO usar em produção.
;
; [database]
; user = admin
; password = SuperSecretPassword123
; host = localhost
; port = 3306

; Observação: o arquivo está comentado para evitar exposição acidental. Em ambientes reais,
; use mecanismos de secret management (AWS Secrets Manager, GitHub Secrets, etc.).
